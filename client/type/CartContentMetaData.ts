type CartContentMetaData = {
  allToggle: boolean;
  maxLength: number;
  totalPrice: number;
  totalDiscount: number;
  checkedCount: number;
  shipmentPrice: number;
};

export type { CartContentMetaData };
