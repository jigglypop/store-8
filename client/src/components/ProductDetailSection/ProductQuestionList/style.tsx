import styled from 'styled-components';

export const ProductQuestionList = styled.div`
  background-color: var(--white);
  margin-top: 45px;
  font-size: var(--body-middle-font);
  color: var(--text-pastel-black);
`;
