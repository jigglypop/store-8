import React, { ReactElement } from 'react';
import * as S from './style';

interface Props {}

function Receipt({}: Props): ReactElement {
  return (
    <S.Receipt>
      <div></div>
    </S.Receipt>
  );
}

export default Receipt;
