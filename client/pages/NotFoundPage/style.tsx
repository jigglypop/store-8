import styled from "styled-components";

// 메인
export const NotFoundPage = styled.div`
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 80vh;
    color: var(--black);
`;
