import styled from '@lib/styledComponent';

export const OrderFinishPage = styled.div`
  width: var(--app-width);
  margin: 0px calc(50vw - var(--app-width) / 2) 0px calc(50vw - var(--app-width) / 2);
  position: relative;
`;
